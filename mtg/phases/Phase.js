if (typeof define !== 'function') { var define = require('amdefine')(module) }

/**
 * A phase of a turn
 * 
 * @author Joe Cavanagh
 **/
define([
    'mtg/steps/Step'
], function(
    Step
) {
    var Phase = function() {

    }

    Phase.prototype = {};

    return Phase;
});
